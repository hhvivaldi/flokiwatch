# Dev-only dependencies (not required for production bot).
#
# Used by:
#   python -m pytest snow/tests/ -v
#
# Production runtime deps live in requirements.txt. Per FLO-347 RFC §12.1
# (CK-3), pytest is scoped to `snow/tests/` only; existing standalone
# scripts in `scripts/` and root-level tests stay untouched.

pytest>=7.4
freezegun>=1.3     # clock mocking for time_between / duration_exceeds (Phase 3+)
